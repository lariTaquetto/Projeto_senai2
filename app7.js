let numero = prompt ("Digite um número");
let numero2 = prompt ("Digite um número");
 
function mensagemNumero (numero, numero2){
    if (numero > numero2){
        return console.log (`O número maior é ${numero}`);
    }
    else (numero2 > numero)
       return console.log (`O número maior é ${numero1}`);
}
mensagemNumero (numero, numero2);